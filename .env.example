# News Digest Agent Environment Configuration

# LM Studio Configuration (Local LLM)
LM_STUDIO_URL=http://host.docker.internal:1234/v1
# LM_STUDIO_MODEL=auto (auto-detect) or specify model name
LM_STUDIO_MODEL=auto
LM_STUDIO_FALLBACK_MODEL=local-model

# Database Configuration (default Docker setup)
DATABASE_URL=postgresql+asyncpg://newsuser:newspass@db:5432/newsdigest

# Redis Configuration (default Docker setup)
REDIS_URL=redis://redis:6379

# Security Configuration
ENABLE_AUTH=false
AUTH_USERNAME=admin
AUTH_PASSWORD=your-secure-password-here
SESSION_SECRET=your-long-random-secret-key-here

# Optional: Custom schedule settings
# UPDATE_INTERVAL_MINUTES=60
# QUIET_HOURS_START=23
# QUIET_HOURS_END=6
# MAX_STORIES_PER_DIGEST=20

# Optional: Custom news sources (comma-separated URLs)
# ADDITIONAL_RSS_FEEDS=https://example.com/feed.rss,https://another.com/rss
